import ReviewModel from "../../models/ReviewModel";
import {Review} from "../Utils/Review";
import {Link} from "react-router-dom";

export const OptionalReturn: React.FC<{
    review: ReviewModel[] | undefined, flight_id: number | undefined, mobile: boolean
}> = (props) => {

    

    return(
       <div></div>
    );
}